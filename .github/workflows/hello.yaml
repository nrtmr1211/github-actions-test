name: Checkout
on: push

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: dump readme
        run: cat README.md

      - uses: ./.github/actions/hello